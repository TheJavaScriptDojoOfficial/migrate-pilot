export const APP_NAME = 'Migrate Pilot';
export const APP_VERSION = '0.0.1';
export const APP_TAGLINE = 'AI executes. Human approves. Git protects.';

/** Local data root (resolved at runtime by the Tauri layer). */
export const LOCAL_DATA_DIR_NAME = '.legacy-modernizer';
